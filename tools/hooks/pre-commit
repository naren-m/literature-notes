#!/bin/sh
set -eu

if [ "${SKIP_BACKLINK_CHECK:-}" = "1" ]; then
  echo "Skipping backlink checks because SKIP_BACKLINK_CHECK=1"
  exit 0
fi

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

exec make pre-commit-check
